REMEMBER:
Misure adottate temporaneamnte per la gestione degli allarmi
1. err_date_missing --> critical, level 2, per avere differenze rispetto a range_alert --> possibilita di invio delle mail ogni volta che accade!
2. il file mail_status viene resettato ogni giorno per permettere l' invio degli errori di pari livello
3. la soluzione migliore sarebbe quella di creare nella tabella del range_file un flag per ogni stazione/variabile che abilita o meno la mail per quel problema ==> da sviluppare in seguito!


