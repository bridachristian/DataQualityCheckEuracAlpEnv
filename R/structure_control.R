structure_control = function(FILE_PATH = scheduling_dir,FILE_NAME = FILE,DATETIME_HEADER = DATETIME_HEADER, DATETIME_FORMAT = DATETIME_FORMAT){

    input_data = read.csv(paste(FILE_PATH,FILE_NAME,sep = ""),header = F,stringsAsFactors = F)
    
    # check if there is row containing only NA
  
}

data[!apply(data == "", 1, all),]

FILE_NAME  = "yM4s.dat"
